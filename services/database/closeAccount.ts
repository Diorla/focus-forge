import "firebase/firestore";
import removeUserCred from "./removeUserCred";

// TODO: Add backend function
export default async function deleteUserData(userId: string) {
  // Trigger backend function to
  /**
   * Delete all user data from firestore
   * Delete user from firestore
   * Delete user from auth
   */
  return removeUserCred();
}
